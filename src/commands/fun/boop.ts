import { Message } from "discord.js";
import { Command } from "../../types";

const boopCommand: Command = {
  name: 'boop',
  description: 'Boops the specified use, as many times as you want.',

  async execute(message: Message) {
    
  }
};

export default boopCommand;